#include <bits/stdc++.h> 
using namespace std;
#define SPEED ios_base::sync_with_stdio(0);

int main() {
    SPEED; 
    int casas[4], dir;
    for (int i=0; i<4; i++){
        cin>>dir;
        casas[i]=dir;
    }
    sort (casas.being(), casas.end());
    
	return 0;
}